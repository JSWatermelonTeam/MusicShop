{{> header}}
    <h2 class="titleForm">Advertisements</h2>
    <div class="row container">
    {{#each adverts}}

        {{> adBox}}
    {{else}}
        <p>No ads in database</p>
    {{/each}}
    </div>
{{> footer}}
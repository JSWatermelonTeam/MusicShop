<table class="table table-striped table-bordered">
    <thead>
    <tr>
        <th>Topic</th>
        {{#if showingRecieved}}
            <th>Sender</th>
        {{else}}
            <th>Reciever</th>
        {{/if}}

        <th>Date composed</th>
        <th></th>
    </tr>
    </thead>
    <tbody>
        {{#each messages}}
            {{>shortRowMessage}}
        {{/each}}
    </tbody>
</table>
